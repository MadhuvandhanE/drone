# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3

# ── Computer vision & AI ───────────────────────────────────────────────────
opencv-python>=4.9.0
ultralytics>=8.1.0       # YOLOv8 – includes torch as a dependency
numpy>=1.26.0

# ── GPU / device selection ─────────────────────────────────────────────────
# torch is installed automatically by ultralytics.
# For CUDA 12.x on RTX 3060, install the matching wheel separately:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0

# ── HTTP client (used by Flutter simulator / test scripts) ─────────────────
httpx>=0.27.0
